class Car {
  constructor (name, picture, status, fuelType, price, currency) {
    this.name = name
    this.picture = picture
    this.status = status
    this.fuelType = fuelType
    this.price = price
    this.currency = currency
  }
}

export default Car
